// const Product = require("../../models").Product;
// const sequelize = require("sequelize");
// const {OPTIONS} = require("../../config/options/global.options");
// const Op = sequelize.Op;
// const generateProductCode = async name => {
//     let count = await Product.count({});
//     return name.substr(0, 2).toUpperCase() + Math.floor(count + Math.random(count) * 900);
// };

// exports.generateProductCode = generateProductCode;